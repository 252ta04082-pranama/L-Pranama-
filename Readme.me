#include <stdio.h>

int main()
{
    float a, b, c, total, average;

    printf("Enter three numbers: ");
    scanf("%f %f %f", &a, &b, &c);

    total = a + b + c;
    average = total / 3;

    printf("Total = %.2f\n", total);
    printf("Average = %.2f", average);

    return 0;
}
